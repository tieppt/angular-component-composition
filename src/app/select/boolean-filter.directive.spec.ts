import { BooleanFilterDirective } from './boolean-filter.directive';

describe('BooleanFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new BooleanFilterDirective();
    expect(directive).toBeTruthy();
  });
});
