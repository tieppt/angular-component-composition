import { CalendarViewDirective } from './calendar-view.directive';

describe('CalendarViewDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarViewDirective();
    expect(directive).toBeTruthy();
  });
});
